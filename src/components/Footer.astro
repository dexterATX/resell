<footer class="site-footer">
  <div class="container site-footer__inner">
    <div class="site-footer__top">
      <div class="site-footer__brand">
        <a class="site-footer__brand-link" href="/" aria-label="FLATFARE home">FLATFARE</a>
        <p>Flat-rate flight deals for travelers who want simple pricing, every destination.</p>
      </div>

      <div class="site-footer__columns">
        <nav aria-label="Company">
          <h2>Company</h2>
          <a href="#">About</a>
          <a href="#">Careers</a>
          <a href="#">Press</a>
        </nav>

        <nav aria-label="Support">
          <h2>Support</h2>
          <a href="#">Help Center</a>
          <a href="#">Contact</a>
          <a href="#">Traveler Safety</a>
        </nav>

        <nav aria-label="Legal">
          <h2>Legal</h2>
          <a href="#">Terms</a>
          <a href="#">Privacy</a>
          <a href="#">Cookie Policy</a>
        </nav>
      </div>
    </div>

    <div class="site-footer__newsletter glass">
      <div>
        <p class="site-footer__eyebrow">Stay ahead of fare resets</p>
        <h2>Get flat-fare drops in your inbox.</h2>
      </div>
      <form class="site-footer__form" action="#" method="post">
        <label class="sr-only" for="newsletter-email">Email address</label>
        <input id="newsletter-email" name="email" type="email" placeholder="you@example.com" autocomplete="email" required />
        <button class="btn btn-primary" type="submit">Subscribe</button>
      </form>
    </div>

    <div class="site-footer__bottom">
      <div class="site-footer__socials" aria-label="Social links">
        <a href="#" aria-label="FLATFARE on Instagram">Instagram</a>
        <a href="#" aria-label="FLATFARE on X">X</a>
        <a href="#" aria-label="FLATFARE on TikTok">TikTok</a>
      </div>
      <p>© {new Date().getFullYear()} FLATFARE. All rights reserved.</p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    position: relative;
    padding-block: var(--space-2xl) var(--space-lg);
    border-top: 1px solid var(--color-border);
    background: rgb(5 5 18 / 0.52);
  }

  .site-footer::before {
    position: absolute;
    inset: 0;
    z-index: -1;
    content: "";
    background: radial-gradient(circle at 50% 0%, rgb(124 58 237 / 0.18), transparent 36%);
    pointer-events: none;
  }

  .site-footer__inner {
    display: grid;
    gap: var(--space-xl);
  }

  .site-footer__top {
    display: grid;
    grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.8fr);
    gap: var(--space-xl);
  }

  .site-footer__brand {
    display: grid;
    gap: var(--space-sm);
    max-width: 26rem;
  }

  .site-footer__brand-link {
    color: var(--color-soft-white);
    font-family: var(--font-display);
    font-size: var(--step-3);
    font-weight: 800;
    letter-spacing: -0.06em;
    text-decoration: none;
  }

  .site-footer__brand p,
  .site-footer__bottom p {
    color: var(--color-fog);
  }

  .site-footer__columns {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: var(--space-md);
  }

  .site-footer__columns nav {
    display: grid;
    gap: var(--space-xs);
  }

  .site-footer__columns h2 {
    color: var(--color-soft-white);
    font-size: var(--step--1);
    letter-spacing: 0.08em;
    line-height: 1.2;
    text-transform: uppercase;
  }

  .site-footer__columns a:not(.site-footer__brand-link) {
    color: var(--color-muted-slate);
    font-size: var(--step--1);
    text-decoration: none;
  }

  .site-footer__columns a:not(.site-footer__brand-link):hover {
    color: var(--color-soft-white);
  }

  .site-footer__newsletter {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-lg);
    padding: clamp(1.2rem, 2.4vw, 2rem);
    overflow: hidden;
  }

  .site-footer__newsletter > div {
    display: grid;
    gap: var(--space-xs);
    max-width: 36rem;
  }

  .site-footer__eyebrow {
    color: var(--color-aero-cyan);
    font-family: var(--font-display);
    font-size: var(--step--1);
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .site-footer__form {
    display: flex;
    width: min(100%, 30rem);
    gap: var(--space-xs);
    padding: var(--space-2xs);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-full);
    background: rgb(255 255 255 / 0.06);
  }

  .site-footer__form input {
    min-width: 0;
    flex: 1;
    border: 0;
    border-radius: var(--radius-full);
    padding-inline: var(--space-sm);
    background: transparent;
    color: var(--color-soft-white);
    outline: 0;
  }

  .site-footer__form input::placeholder {
    color: var(--color-muted-slate);
  }

  .site-footer__form button {
    white-space: nowrap;
  }

  .site-footer__bottom {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-md);
    padding-block-start: var(--space-lg);
    border-top: 1px solid rgb(248 250 252 / 0.1);
    font-size: var(--step--1);
  }

  .site-footer__socials {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-sm);
  }

  .site-footer__socials a {
    color: var(--color-fog);
    text-decoration: none;
  }

  .site-footer__socials a:hover {
    color: var(--color-soft-white);
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  @media (max-width: 56rem) {
    .site-footer__top,
    .site-footer__newsletter {
      grid-template-columns: 1fr;
    }

    .site-footer__newsletter {
      align-items: stretch;
      flex-direction: column;
    }

    .site-footer__form {
      width: 100%;
    }
  }

  @media (max-width: 42rem) {
    .site-footer__columns {
      grid-template-columns: 1fr;
    }

    .site-footer__bottom {
      align-items: flex-start;
      flex-direction: column;
    }

    .site-footer__form {
      flex-direction: column;
      border-radius: var(--radius-lg);
    }

    .site-footer__form input {
      min-height: 2.75rem;
    }
  }
</style>
